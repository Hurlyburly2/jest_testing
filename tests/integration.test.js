const axios = require('axios')
const routes = require('./getRoutes')

/**
 * Integration tests
 * @group integration
 */
 describe('Not a real integration test!', () => {
    test('three plus three is six', () => {
        expect(3 + 3).toBe(6)
    })
})